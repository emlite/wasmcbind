#include <webbind/HTMLHeadElement.h>


DEFINE_EMLITE_TYPE(HTMLHeadElement, HTMLElement);


HTMLHeadElement HTMLHeadElement_new() : HTMLElement(em_Val_global("HTMLHeadElement").new_()) {
        return HTMLHeadElement(em_Val_new(em_Val_global("HTMLHeadElement", ));
      }

